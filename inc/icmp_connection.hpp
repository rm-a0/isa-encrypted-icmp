#ifndef ICMP_CONNECTION_HPP
#define ICMP_CONNECTION_HPP

#include <string>
#include <vector>

/**
 * @class ICMPConnection
 * @brief 
 */
class ICMPConnection {
public:
    ICMPConnection(void);
    ~ICMPConnection();
private:
};

#endif // ICMP_CONNECTION_HPP